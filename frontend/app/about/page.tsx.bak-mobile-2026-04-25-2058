'use client'
import { useState, useEffect } from 'react'
import { initParticlesEngine, Particles } from "@tsparticles/react";
import { loadSlim } from "@tsparticles/slim";
import { HeaderSection } from './components/HeaderSection'
import { ContactSection } from './components/ContactSection'
import { GallerySection } from './components/GallerySection'
import { HeroSection } from './components/HeroSection'
import AboutLongSeoBlocks from './long-seo-blocks/AboutLongSeoBlocks'
import AboutSeoBlocks from './seo-blocks/AboutSeoBlocks'
import { FounderSeoSection, ProductToolsSection } from './components/FounderSeoSection' 

export default function AboutPage() {
  const [lang, setLang] = useState<"ru" | "uz" | "en">("ru")
  const [init, setInit] = useState(false);

  const texts = {
    ru: {
      title: "MarketCard AI",
      subtitle: "AI-платформа для создания контента и аналитики для маркетплейсов",
      platform: "Перейти на платформу",
      support: "Поддержка",
    },
    uz: {
      title: "MarketCard AI",
      subtitle: "Marketpleyslar uchun kontent va tahlil yaratadigan AI platforma",
      platform: "Platformaga o'tish",
      support: "Qo'llab-quvvatlash",
    },
    en: {
      title: "MarketCard AI",
      subtitle: "AI platform for marketplace content creation and analytics",
      platform: "Go to platform",
      support: "Support",
    },
  }[lang]


  useEffect(() => {
    initParticlesEngine(async (engine) => { await loadSlim(engine); }).then(() => setInit(true));
  }, []);

  return (
    <main style={{ minHeight: "100vh", backgroundColor: "#05070a", color: "#ffffff", padding: "40px 20px", position: "relative", fontFamily: "'Inter', sans-serif" }}>
      {init && <Particles style={{ position: "absolute", top: 0, left: 0, zIndex: 0 }} options={{ particles: { color: { value: "#007bff" }, links: { color: "#007bff", distance: 200, enable: true, opacity: 0.8, width: 1 }, move: { enable: true, speed: 0.5 }, number: { value: 60 }, opacity: { value: 0.9 }, size: { value: 3 } } }} />}
      
      <div style={{ maxWidth: "1100px", margin: "0 auto", position: "relative", zIndex: 1 }}>
        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "40px" }}>
          <a href="https://marketcard.uz/" style={{ backgroundColor: "#1a1f2e", color: "#ffffff", padding: "10px 20px", borderRadius: "8px", textDecoration: "none", border: "1px solid #3b82f6", fontWeight: "bold" }}>{texts.platform}</a>
          <select
            value={lang}
            onChange={(e) => setLang(e.target.value as "ru" | "uz" | "en")}
            style={{
              background: "rgba(15,23,42,0.92)",
              color: "white",
              border: "1px solid #3b82f6",
              borderRadius: "12px",
              padding: "11px 18px",
              fontWeight: "bold",
              cursor: "pointer",
              outline: "none",
              boxShadow: "0 0 24px rgba(59,130,246,0.35)"
            }}
          >
            <option value="ru">🇷🇺 Русский</option>
            <option value="uz">🇺🇿 O‘zbekcha</option>
            <option value="en">🇬🇧 English</option>
          </select>
        </div>
        
        <HeaderSection title={texts.title} subtitle={texts.subtitle} />
        <ContactSection title={texts.support} />
        <section style={{ backgroundColor: "#0b101d", padding: "40px", borderRadius: "20px", border: "1px solid #2d3748", marginBottom: "40px" }}>
<div style={{
  fontSize: "32px",
  fontWeight: "900",
  color: "#ffffff",
  marginBottom: "20px",
  marginTop: "40px"
}}>
  Примеры наших работ
</div>

<GallerySection />
</section>
        <section style={{ backgroundColor: "#0b101d", padding: "40px", borderRadius: "20px", border: "1px solid #2d3748", marginBottom: "40px" }}><HeroSection />
<AboutLongSeoBlocks lang={lang} />
</section>
        






      </div>
    </main>
  )
}
